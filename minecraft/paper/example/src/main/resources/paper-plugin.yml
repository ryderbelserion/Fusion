name: 'Fusion'
main: 'com.ryderbelserion.fusion.Fusion'

version: '1.0.0'
api-version: '1.21.11'
description: 'A test plugin for Fusion API'

authors: [ ryderbelserion ]

folia-supported: true

dependencies:
  server:
    DecentHolograms:
      load: BEFORE
      required: false
      join-classpath: true
    FancyHolograms:
      load: BEFORE
      required: false
      join-classpath: true
    CMI:
      load: BEFORE
      required: false
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true
    ItemsAdder:
      load: BEFORE
      required: false
      join-classpath: true
    Oraxen:
      load: BEFORE
      required: false
      join-classpath: true
    Nexo:
      load: BEFORE
      required: false
      join-classpath: true
    HeadDatabase:
      load: BEFORE
      required: false
      join-classpath: true