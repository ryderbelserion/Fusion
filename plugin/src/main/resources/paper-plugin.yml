name: 'Fusion'
main: 'com.ryderbelserion.fusion.Fusion'
bootstrapper: 'com.ryderbelserion.fusion.FusionLoader'

version: '1.0.0'
api-version: '1.21.10'
description: 'A library for the ages!'

folia-supported: true

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true
    HeadDatabase:
      load: BEFORE
      required: false
      join-classpath: true